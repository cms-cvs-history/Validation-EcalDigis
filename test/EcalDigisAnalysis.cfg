process EcalDigisAnalysis  = 
	    {

  untracked PSet maxEvents = {untracked int32 input = -1}

            source = PoolSource { 
        	untracked vstring fileNames = {'file:digis.root'}
          	untracked uint32 debugVebosity = 10
          	untracked bool   debugFlag     = true
		}

  #Geometry
  #
  include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"  

  include "Validation/EcalDigis/data/ecalDigisValidationSequence.cff"

  path p1 = { ecalDigisValidationSequence }

  service = MessageLogger {     untracked vstring destinations = {"cout"}
#    untracked vstring categories = { "HitInfo","DigiInfo" }
#    untracked vstring debugModules = {"ecalDigisValidation"}
    untracked PSet cout = { 
#      untracked string threshold = "DEBUG" 
#      untracked PSet DEBUG = { untracked int32 limit = 0 }
      untracked PSet INFO = { untracked int32 limit = -1 }
#      untracked PSet HitInfo = { untracked int32 limit = 10000000 }
#      untracked PSet DigiInfo = { untracked int32 limit = 10000000 }
    }
  } 
                                                                                
  service = DaqMonitorROOTBackEnd{ }
	
  }		
